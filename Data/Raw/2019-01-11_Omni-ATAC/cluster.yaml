__default__:
  params: '-cwd -V -q light.q'

trim:
  params: '-cwd -V -q lupiengroup -l h_vmem=12G -l mem_requested=12G'

align:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

subsample_tagalign:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

gen_pseudo:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

callpeak:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

slop_pval:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

slop_fold:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

bdgcmpp:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'

bdgcmp_signalval:
  params: '-cwd -V -q lupiengroup -l h_vmem=16G -l mem_requested=16G'